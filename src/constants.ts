/** ルームに入れる人数の上限（CLAUDE.md セクション1: 2〜4人）。 */
export const MAX_PLAYERS = 4;

/** ゲーム開始に必要な最小人数。 */
export const MIN_PLAYERS = 2;

/** 既定のターン数（CLAUDE.md セクション4: maxTurns 既定 10）。 */
export const DEFAULT_MAX_TURNS = 10;

/** プレイヤー名の最大文字数。 */
export const MAX_NAME_LENGTH = 8;
